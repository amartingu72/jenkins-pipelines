pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            dir('/apiconnect/'){
                steps {
                    sh 'node --version'
                    sh 'svn --version'
                }
            }
        }
    }
}

