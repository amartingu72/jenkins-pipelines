pipeline {
    agent { 
        docker { image 'apiconnect-agent:latest' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
          
        }
    }
}

